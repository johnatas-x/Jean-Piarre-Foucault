{
  "regexManagers": [
    {
      "fileMatch": ["^\\.env\\.example$"],
      "matchStrings": [
        "PHP_TAG=(?<currentValue>[\\w\\.-]+)",
        "MARIADB_TAG=(?<currentValue>[\\w\\.-]+)",
        "NGINX_TAG=(?<currentValue>[\\w\\.-]+)",
        "VARNISH_TAG=(?<currentValue>[\\w\\.-]+)",
        "MEMCACHED_TAG=(?<currentValue>[\\w\\.-]+)",
        "PYTHON_TAG=(?<currentValue>[\\w\\.-]+)",
        "TRAEFIK_TAG=(?<currentValue>[\\w\\.-]+)"
      ],
      "datasourceTemplate": "docker",
      "depNameTemplate": "{{#if (eq matchString 'PHP_TAG')}}wodby/drupal-php{{/if}}{{#if (eq matchString 'MARIADB_TAG')}}wodby/mariadb{{/if}}{{#if (eq matchString 'NGINX_TAG')}}wodby/nginx{{/if}}{{#if (eq matchString 'VARNISH_TAG')}}wodby/varnish{{/if}}{{#if (eq matchString 'MEMCACHED_TAG')}}wodby/memcached{{/if}}{{#if (eq matchString 'PYTHON_TAG')}}python{{/if}}{{#if (eq matchString 'TRAEFIK_TAG')}}traefik{{/if}}",
      "extractVersionTemplate": "{{#if (eq matchString 'PYTHON_TAG')}}{{currentValue}}-alpine{{else}}{{currentValue}}{{/if}}",
      "versioningTemplate": "loose"
    }
  ]
}
